import type { Request, Response } from "express";

const uploadToCloud = async (req: Request, res: Response) => {};
const deleteFromCloud = async (req: Request, res: Response) => {};
export { uploadToCloud, deleteFromCloud };
